import { SearchEngine } from "../../commons";


const CareerSearch = () => {
  window.scrollTo(0, 0);

  return (
    <div>
      Career Search is working!
      <SearchEngine />
    </div>
  );
};

export default CareerSearch;
